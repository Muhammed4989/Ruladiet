/* ========================================
   RULA DIET - COMPLETE CUSTOM STYLESHEET
   Combined from Vercel style.css + pages.css
   ======================================== */

:root {
    --primary: #2D5240;
    --primary-light: #3A6B54;
    --primary-dark: #1F3A2D;
    --accent: #C8A97E;
    --accent-hover: #B8956A;
    --bg: #F7F9F5;
    --bg-alt: #E8F5EE;
    --text: #1A2E24;
    --text-light: #4A6B5A;
    --text-body: #4A6B5A;
    --text-muted: #7A7A7A;
    --text-faint: #8BA69A;
    --border: #D4E3D9;
    --white: #FFFFFF;
    --shadow: 0 4px 24px rgba(45,82,64,0.08);
    --radius: 16px;
    --radius-sm: 10px;
    --radius-lg: 24px;
    --radius-xl: 32px;
    --transition: 0.3s ease;
    --container: 1160px;
    --font: 'Tajawal', sans-serif;
    --font-primary: 'Tajawal', sans-serif;
    --primary-glow: rgba(45,82,64,0.08);
    --accent-light: #F5EBE0;
    --bg-cream: #FAFAF7;
    --bg-warm: #F6F4F0;
    --border-light: #E8E5E0;
    --shadow-sm: 0 1px 3px rgba(0,0,0,0.04);
    --shadow-md: 0 4px 16px rgba(0,0,0,0.06);
    --shadow-lg: 0 12px 48px rgba(0,0,0,0.08);
    --shadow-xl: 0 24px 64px rgba(0,0,0,0.1);
    --brand-green: #5F8F73;
    --brand-mint: #8FA79A;
    --brand-light-green: #CBE6C7;
    --brand-off-white: #F7F9F5;
    --brand-beige: #E8E1D9;
    --brand-red: #8B1E20;
    --brand-gold: #EBC252;
    --highlight-green: #A8E6CF;
}

/* Rula Custom Pages Container */
.rula-page {
    font-family: 'Tajawal', sans-serif;
    direction: rtl;
}

.rula-page img {
    max-width: 100%;
    height: auto;
    display: block;
}

.rula-page .container {
    max-width: 1160px;
    margin: 0 auto;
    padding: 0 28px;
}

/* Section Styles */
.rula-page .section-header {
    text-align: center;
    margin-bottom: 56px;
}

.rula-page .section-badge {
    display: inline-block;
    background: rgba(45, 82, 64, 0.1);
    color: var(--primary-dark);
    font-size: 0.85rem;
    font-weight: 600;
    padding: 6px 18px;
    border-radius: 50px;
    margin-bottom: 16px;
    letter-spacing: 0.3px;
}

.rula-page .section-title {
    font-size: clamp(1.75rem, 3.5vw, 2.25rem);
    font-weight: 800;
    color: var(--text);
    margin-bottom: 12px;
    line-height: 1.3;
}

.rula-page .section-desc {
    color: var(--text-muted);
    font-size: 1.05rem;
    max-width: 520px;
    margin: 0 auto;
    line-height: 1.7;
}

/* Buttons */
.rula-page .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 14px 32px;
    border-radius: 50px;
    font-family: 'Tajawal', sans-serif;
    font-size: 0.95rem;
    font-weight: 600;
    border: 2px solid transparent;
    cursor: pointer;
    transition: all 0.3s ease;
    white-space: nowrap;
    letter-spacing: 0.2px;
}

.rula-page .btn-primary {
    background: var(--primary);
    color: #fff;
    border-color: var(--primary);
    box-shadow: 0 4px 16px rgba(61, 107, 79, 0.25);
}

.rula-page .btn-primary:hover {
    background: var(--primary-dark);
    border-color: var(--primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(61, 107, 79, 0.3);
}

.rula-page .btn-outline {
    background: transparent;
    color: var(--primary);
    border-color: var(--primary);
}

.rula-page .btn-outline:hover {
    background: var(--primary);
    color: #fff;
    transform: translateY(-2px);
}

.rula-page .btn-lg {
    padding: 18px 40px;
    font-size: 1.05rem;
}

.rula-page .badge-free {
    background: var(--primary-light);
    color: var(--primary);
    font-size: 0.8rem;
    padding: 3px 12px;
    border-radius: 50px;
    font-weight: 600;
}

/* Hero Section */
.rula-hero {
    position: relative;
    padding: 0;
    overflow: visible;
    height: 80vh;
    max-height: 650px;
    display: flex;
    align-items: center;
}

.rula-hero .hero-wave {
    position: absolute;
    bottom: -1px;
    left: 0;
    right: 0;
    height: 80px;
    z-index: 2;
    line-height: 0;
    overflow: hidden;
    pointer-events: none;
}

.rula-hero .hero-wave svg {
    width: 100%;
    height: 100%;
    display: block;
    fill: var(--bg);
}

.rula-hero .section-wave {
    height: 100px;
    line-height: 0;
    overflow: hidden;
    pointer-events: none;
}

.rula-hero .section-wave svg {
    width: 100%;
    height: 100%;
    display: block;
}

.rula-hero .wave-green {
    fill: var(--primary);
    opacity: 0.08;
}

.rula-hero .wave-white {
    fill: var(--bg);
}

.rula-hero .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
}

.rula-hero .hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
}

.rula-hero .hero-bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(100deg,
        rgba(26, 26, 26, 0.85) 0%,
        rgba(61, 107, 79, 0.5) 50%,
        rgba(26, 26, 26, 0.8) 100%);
}

.rula-hero .hero-wrapper {
    position: relative;
    z-index: 1;
    width: 100%;
    padding: 0 28px;
}

.rula-hero .hero-content {
    position: relative;
    z-index: 1;
    padding: 60px 0;
    width: 100%;
    max-width: 1160px;
    margin: 0 auto;
}

.rula-hero .hero-text-col {
    color: #fff;
    max-width: 620px;
    text-align: right;
    margin-right: 0;
    margin-left: auto;
    padding-right: 20px;
}

.rula-hero .hero-text-col .hero-subtitle,
.rula-hero .hero-text-col .hero-title,
.rula-hero .hero-text-col .hero-text {
    margin-right: 0;
    margin-left: auto;
    padding-right: 0;
}

.rula-hero .hero-text-col .hero-actions {
    justify-content: flex-start;
}

.rula-hero .hero-text-col .hero-stats {
    justify-content: flex-end;
}

.rula-hero .hero-text-col .hero-stats .stat {
    text-align: center;
}

.rula-hero .hero-subtitle {
    display: inline-block;
    font-size: 0.95rem;
    color: #fff;
    font-weight: 600;
    margin-bottom: 20px;
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(10px);
    padding: 8px 22px;
    border-radius: 50px;
    border: 1px solid rgba(255,255,255,0.2);
    text-align: center;
}

.rula-hero .hero-title {
    font-size: clamp(2rem, 4.5vw, 3rem);
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 16px;
    color: #fff;
    letter-spacing: -0.3px;
}

.rula-hero .hero-title .highlight {
    color: var(--highlight-green);
    position: relative;
    display: inline-block;
}

.rula-hero .hero-title .highlight::after {
    content: '';
    position: absolute;
    bottom: 4px;
    right: 0;
    left: 0;
    height: 7px;
    background: rgba(168, 230, 207, 0.2);
    border-radius: 4px;
    z-index: -1;
}

.rula-hero .hero-text {
    font-size: 1.1rem;
    color: rgba(255,255,255,0.85);
    max-width: 560px;
    margin: 0 0 32px 0;
    line-height: 1.8;
}

.rula-hero .hero-actions {
    display: flex;
    gap: 14px;
    justify-content: flex-end;
    flex-wrap: wrap;
    margin-bottom: 40px;
}

.rula-hero .hero-actions .btn-primary {
    background: #fff;
    color: var(--primary);
    border-color: #fff;
    box-shadow: 0 4px 16px rgba(0,0,0,0.15);
}

.rula-hero .hero-actions .btn-primary:hover {
    background: var(--primary-light);
    transform: translateY(-2px);
}

.rula-hero .hero-actions .btn-outline {
    color: #fff;
    border-color: rgba(255,255,255,0.4);
}

.rula-hero .hero-actions .btn-outline:hover {
    background: #fff;
    color: var(--primary);
    border-color: #fff;
}

.rula-hero .hero-stats {
    display: flex;
    justify-content: flex-end;
    gap: 24px;
    flex-wrap: wrap;
    padding-top: 32px;
    padding-right: 20px;
    border-top: 1px solid rgba(255,255,255,0.12);
}

.rula-hero .stat {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 10px;
    height: auto;
    padding: 20px;
    text-align: center;
    min-width: 0;
}

.rula-hero .stat-number {
    display: block;
    font-size: 1.35rem;
    font-weight: 800;
    color: #fff;
    line-height: 1.2;
}

.rula-hero .stat-label {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.65);
    line-height: 1.4;
    text-align: center;
}

@media (max-width: 768px) {
    .rula-hero {
        height: auto;
        max-height: none;
        min-height: 60vh;
    }
    .rula-hero .hero-content {
        padding: 40px 0;
    }
    .rula-hero .hero-text-col {
        text-align: center;
        max-width: 100%;
    }
    .rula-hero .hero-text-col .hero-subtitle,
    .rula-hero .hero-text-col .hero-title,
    .rula-hero .hero-text-col .hero-text {
        margin-right: auto;
        margin-left: auto;
    }
    .rula-hero .hero-text-col .hero-actions,
    .rula-hero .hero-text-col .hero-stats {
        justify-content: center;
    }
    .rula-hero .hero-text-col .hero-stats .stat {
        text-align: center;
    }
    .rula-hero .hero-wave {
        height: 50px;
    }
    .rula-hero .section-wave {
        height: 50px;
    }
}

/* BMI Section */
.rula-page .bmi-section {
    position: relative;
    padding: 48px 0;
    background: #eef3f1;
}

.rula-page .bmi-container {
    max-width: 1160px;
    margin: 0 auto;
    padding: 0 28px;
}

.rula-page .bmi-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: stretch;
}

.rula-page .bmi-left,
.rula-page .bmi-right {
    background: #fff;
    border-radius: 24px;
    padding: 36px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.03);
    display: flex;
    flex-direction: column;
}

.rula-page .bmi-left {
    min-height: 480px;
}

.rula-page .about-mini {
    flex: 1;
    display: flex;
    flex-direction: column;
}

.rula-page .about-mini-content {
    display: flex;
    gap: 28px;
    align-items: flex-start;
    margin-top: 16px;
    flex: 1;
}

.rula-page .about-mini-image {
    flex-shrink: 0;
    width: 160px;
    height: 200px;
    border-radius: 16px;
    overflow: hidden;
}

.rula-page .about-mini-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.rula-page .about-mini-text {
    flex: 1;
}

.rula-page .about-mini-text h3 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 12px;
}

.rula-page .credentials-mini {
    list-style: none;
    padding: 0;
    margin: 0;
}

.rula-page .credentials-mini li {
    font-size: 0.85rem;
    color: var(--text-body);
    padding: 8px 0;
    border-bottom: 1px solid var(--border-light);
    position: relative;
    padding-right: 18px;
}

.rula-page .credentials-mini li:last-child {
    border-bottom: none;
}

.rula-page .credentials-mini li::before {
    content: '';
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 6px;
    height: 6px;
    background: var(--primary);
    border-radius: 50%;
}

.rula-page .bmi-right {
    min-height: 480px;
}

.rula-page .bmi-card {
    background: #fff;
    border-radius: 24px;
    box-shadow: 0 2px 16px rgba(0,0,0,0.03);
}

.rula-page .bmi-card-inner {
    padding: 32px 28px;
}

.rula-page .bmi-label {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.85rem;
    color: var(--text-muted);
    font-weight: 500;
    margin-bottom: 12px;
}

.rula-page .bmi-label svg {
    color: var(--accent);
}

.rula-page .bmi-title {
    font-size: clamp(1.3rem, 2.5vw, 1.65rem);
    font-weight: 800;
    color: var(--text);
    margin-bottom: 24px;
    line-height: 1.4;
}

.rula-page .bmi-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 10px;
}

.rula-page .form-group label {
    display: block;
    font-weight: 600;
    margin-bottom: 6px;
    font-size: 0.85rem;
    color: var(--text);
}

.rula-page .form-group input {
    width: 100%;
    height: 52px;
    padding: 0 16px;
    border: 1.5px solid #e8e8e8;
    border-radius: 12px;
    font-family: 'Tajawal', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    text-align: center;
    background: #fafafa;
    color: var(--text);
    transition: all 0.2s ease;
    box-sizing: border-box;
}

.rula-page .form-group input::placeholder {
    color: #bbb;
    font-weight: 400;
}

.rula-page .form-group input:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 4px rgba(61, 107, 79, 0.08);
    background: #fff;
}

.rula-page .bmi-helper {
    font-size: 0.8rem;
    color: var(--text-faint);
    margin-bottom: 20px;
}

.rula-page .bmi-result {
    background: #f8f8f8;
    border-radius: 14px;
    padding: 18px 20px;
    margin-bottom: 20px;
}

.rula-page .result-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
}

.rula-page .result-value,
.rula-page .result-class {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.rula-page .result-value {
    text-align: right;
}

.rula-page .result-class {
    text-align: left;
}

.rula-page .result-label {
    font-size: 0.75rem;
    color: var(--text-faint);
    font-weight: 500;
}

.rula-page .result-number {
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--text);
    line-height: 1;
}

.rula-page .result-badge {
    display: inline-block;
    padding: 5px 12px;
    border-radius: 50px;
    font-size: 0.8rem;
    font-weight: 600;
    background: #fee2e2;
    color: #dc2626;
}

.rula-page .result-badge.normal {
    background: #d1fae5;
    color: #059669;
}

.rula-page .result-badge.overweight {
    background: #fef3c7;
    color: #d97706;
}

.rula-page .result-badge.obese {
    background: #fee2e2;
    color: #dc2626;
}

.rula-page .result-badge.underweight {
    background: #dbeafe;
    color: #2563eb;
}

.rula-page .bmi-scale {
    margin-bottom: 12px;
}

.rula-page .scale-bar {
    height: 8px;
    border-radius: 8px;
    overflow: hidden;
    display: flex;
    margin-bottom: 6px;
}

.rula-page .scale-segment {
    height: 100%;
    flex: 1;
}

.rula-page .scale-yellow { background: #fbbf24; }
.rula-page .scale-green { background: #34d399; }
.rula-page .scale-blue { background: #60a5fa; }
.rula-page .scale-red { background: #f87171; }

.rula-page .scale-markers {
    display: flex;
    justify-content: space-between;
    font-size: 0.7rem;
    color: var(--text-faint);
    font-weight: 500;
    direction: ltr;
}

.rula-page .bmi-motivation {
    font-size: 0.8rem;
    color: var(--text-muted);
    margin-bottom: 0;
}

.rula-page .bmi-actions {
    display: flex;
    gap: 10px;
    margin-top: 18px;
    margin-bottom: 14px;
}

.rula-page .bmi-actions .btn {
    flex: 1;
    height: 48px;
    border-radius: 50px;
    font-size: 0.95rem;
    font-weight: 600;
}

.rula-page .btn-secondary {
    background: #f5f5f5;
    color: var(--text);
    border: 1.5px solid #e8e8e8;
}

.rula-page .btn-secondary:hover {
    background: #eaeaea;
    border-color: #ddd;
}

.rula-page .bmi-note {
    font-size: 0.75rem;
    color: var(--text-faint);
    line-height: 1.5;
    text-align: center;
}

@media (max-width: 900px) {
    .rula-page .bmi-grid {
        grid-template-columns: 1fr;
        gap: 24px;
    }
    .rula-page .bmi-left,
    .rula-page .bmi-right {
        padding: 28px;
        min-height: auto;
    }
    .rula-page .about-mini-content {
        flex-direction: column;
        align-items: center;
        text-align: center;
    }
    .rula-page .about-mini-image {
        width: 120px;
        height: 150px;
    }
}

@media (max-width: 500px) {
    .rula-page .bmi-section {
        padding: 32px 0;
    }
    .rula-page .bmi-container {
        padding: 0 16px;
    }
    .rula-page .bmi-form {
        grid-template-columns: 1fr;
    }
    .rula-page .bmi-card-inner {
        padding: 24px 18px;
    }
    .rula-page .bmi-card {
        border-radius: 20px;
    }
    .rula-page .result-header {
        flex-direction: column;
        gap: 12px;
    }
    .rula-page .result-class {
        text-align: right;
    }
}

/* Courses Section */
.rula-page .courses-section {
    padding: 40px 0 48px;
    background: var(--bg);
}

.rula-page .courses-grid {
    display: flex;
    gap: 24px;
    margin-bottom: 48px;
    padding-bottom: 8px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
}

.rula-page .courses-grid::-webkit-scrollbar {
    height: 6px;
}

.rula-page .courses-grid::-webkit-scrollbar-track {
    background: var(--border-light);
    border-radius: 3px;
}

.rula-page .courses-grid::-webkit-scrollbar-thumb {
    background: var(--brand-green);
    border-radius: 3px;
}

.rula-page .course-card {
    background: #fff;
    border-radius: var(--radius);
    overflow: hidden;
    transition: all 0.3s ease;
    display: block;
    border: 1px solid var(--border-light);
    position: relative;
    flex: 0 0 380px;
    scroll-snap-align: start;
}

.rula-page .course-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-xl);
    border-color: transparent;
}

.rula-page .course-image {
    aspect-ratio: 16/10;
    overflow: hidden;
    position: relative;
}

.rula-page .course-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
}

.rula-page .course-card:hover .course-image img {
    transform: scale(1.08);
}

.rula-page .course-image::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.15) 0%, transparent 50%);
    pointer-events: none;
}

.rula-page .course-body {
    padding: 24px;
    padding-left: 90px;
    position: relative;
}

.rula-page .course-rating {
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    gap: 8px;
}

.rula-page .stars {
    color: #F59E0B;
    font-size: 0.85rem;
    letter-spacing: 1px;
}

.rula-page .rating-text {
    font-size: 0.82rem;
    color: var(--text-faint);
}

.rula-page .course-title {
    font-size: 1.08rem;
    font-weight: 700;
    margin-bottom: 8px;
    line-height: 1.45;
    color: var(--text);
}

.rula-page .course-meta {
    display: flex;
    gap: 14px;
    margin-bottom: 16px;
    font-size: 0.82rem;
    color: var(--text-faint);
    font-weight: 500;
}

.rula-page .course-price {
    position: absolute;
    top: 0;
    left: 16px;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    z-index: 5;
    background: var(--primary);
    color: #fff;
    border-radius: 50%;
    width: 64px;
    height: 64px;
    justify-content: center;
    line-height: 1.2;
    box-shadow: 0 3px 10px rgba(0,0,0,0.15);
    text-align: center;
    padding: 4px;
}

.rula-page .price-current {
    font-size: 0.85rem;
    font-weight: 700;
    color: #fff;
}

.rula-page .price-old {
    font-size: 0.6rem;
    color: rgba(255,255,255,0.7);
    text-decoration: line-through;
}

.rula-page .price-free {
    font-size: 0.75rem;
    font-weight: 700;
    color: #fff;
}

.rula-page .section-cta {
    text-align: center;
}

/* Courses Scroll Buttons */
.rula-page .courses-wrapper {
    position: relative;
}

.rula-page .scroll-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: #fff;
    border: 1px solid var(--border-light);
    box-shadow: var(--shadow-md);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    color: var(--text);
    transition: all 0.3s ease;
}

.rula-page .scroll-btn:hover {
    background: var(--primary);
    color: #fff;
    border-color: var(--primary);
}

.rula-page .scroll-btn--left {
    left: -18px;
}

.rula-page .scroll-btn--right {
    right: -18px;
}

/* Media Section */
.rula-page .media-section {
    padding: 48px 0;
    background: var(--bg-cream);
}

.rula-page .media-logos {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 40px;
    padding-bottom: 8px;
}

.rula-page .media-logos img {
    height: 48px;
    width: auto;
    opacity: 0.85;
    filter: none;
    transition: all 0.3s ease;
    flex: 0 0 auto;
    scroll-snap-align: center;
}

.rula-page .media-logos img:hover {
    opacity: 1;
    transform: scale(1.05);
}

/* Blog Section */
.rula-page .blog-section {
    padding: 60px 0;
    background: var(--bg);
}

.rula-page .blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
    gap: 28px;
    margin-bottom: 48px;
}

.rula-page .blog-card {
    background: #fff;
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s ease;
    border: 1px solid var(--border-light);
}

.rula-page .blog-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-xl);
    border-color: transparent;
}

.rula-page .blog-link {
    display: block;
    height: 100%;
}

.rula-page .blog-image {
    aspect-ratio: 16/10;
    overflow: hidden;
}

.rula-page .blog-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
}

.rula-page .blog-card:hover .blog-image img {
    transform: scale(1.06);
}

.rula-page .blog-body {
    padding: 28px;
}

.rula-page .blog-meta {
    display: flex;
    gap: 12px;
    align-items: center;
    margin-bottom: 14px;
}

.rula-page .blog-date {
    font-size: 0.82rem;
    color: var(--text-faint);
}

.rula-page .blog-category {
    background: rgba(45, 82, 64, 0.1);
    color: var(--primary-dark);
    padding: 3px 12px;
    border-radius: 50px;
    font-size: 0.78rem;
    font-weight: 600;
}

.rula-page .blog-title {
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 10px;
    line-height: 1.5;
    color: var(--text);
}

.rula-page .blog-excerpt {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
    margin-bottom: 14px;
}

.rula-page .blog-read-more {
    color: var(--primary);
    font-weight: 600;
    font-size: 0.92rem;
    display: inline-flex;
    align-items: center;
    gap: 6px;
}

.rula-page .blog-read-more::after {
    content: '\2190';
    transition: transform 0.3s ease;
}

.rula-page .blog-card:hover .blog-read-more::after {
    transform: translateX(4px);
}

/* CTA Section */
.rula-page .cta-section {
    padding: 60px 0;
    background: var(--bg-cream);
}

.rula-page .cta-card {
    background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
    border-radius: var(--radius-xl);
    padding: 72px 48px;
    text-align: center;
    color: #fff;
    position: relative;
    overflow: hidden;
}

.rula-page .cta-card::before {
    content: '';
    position: absolute;
    top: -40%;
    right: -20%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
}

.rula-page .cta-title {
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    font-weight: 800;
    margin-bottom: 16px;
    position: relative;
    z-index: 1;
}

.rula-page .cta-text {
    font-size: 1.1rem;
    opacity: 0.9;
    margin-bottom: 36px;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
    line-height: 1.7;
}

.rula-page .cta-card .btn {
    background: #fff;
    color: var(--primary);
    border-color: #fff;
    position: relative;
    z-index: 1;
}

.rula-page .cta-card .btn:hover {
    background: var(--bg-cream);
    transform: translateY(-2px);
}

/* Testimonials Section */
.rula-page .testimonials-section {
    padding: 48px 0;
    background: #fff;
}

.rula-page .testimonials-header {
    text-align: center;
    margin-bottom: 48px;
}

.rula-page .testimonials-badge {
    display: inline-block;
    background: rgba(45, 82, 64, 0.1);
    color: var(--primary-dark);
    font-size: 0.85rem;
    font-weight: 600;
    padding: 6px 20px;
    border-radius: 50px;
    margin-bottom: 16px;
}

.rula-page .testimonials-title {
    font-size: clamp(1.8rem, 3.5vw, 2.4rem);
    font-weight: 800;
    color: var(--text);
    margin-bottom: 16px;
    line-height: 1.3;
}

.rula-page .testimonials-divider {
    width: 60px;
    height: 3px;
    background: var(--primary);
    border-radius: 2px;
    margin: 0 auto 16px;
}

.rula-page .testimonials-desc {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    color: var(--text-muted);
    font-size: 0.9rem;
}

/* Slider */
.rula-page .testimonials-slider {
    position: relative;
    overflow: hidden;
    margin-bottom: 32px;
}

.rula-page .slider-track {
    display: flex;
    gap: 24px;
    transition: transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    direction: ltr;
    padding: 12px 0;
}

.rula-page .slider-loading {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 16px;
    width: 100%;
    padding: 60px 0;
    color: var(--text-muted);
    font-size: 0.95rem;
}

.rula-page .loading-spinner {
    width: 32px;
    height: 32px;
    border: 3px solid #e8e8e8;
    border-top-color: var(--primary);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

.rula-page .slider-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 44px;
    height: 44px;
    border-radius: 50%;
    border: none;
    background: #fff;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    color: var(--text);
    font-size: 1.1rem;
    cursor: pointer;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
}

.rula-page .slider-arrow:hover {
    background: var(--primary);
    color: #fff;
    box-shadow: 0 4px 20px rgba(61, 107, 79, 0.2);
}

.rula-page .slider-arrow--right {
    right: -8px;
}

.rula-page .slider-arrow--left {
    left: -8px;
}

.rula-page .testimonial-card {
    flex: 0 0 calc(33.333% - 16px);
    min-width: 300px;
    background: #fff;
    border: 1px solid #eee;
    border-radius: 20px;
    padding: 40px 28px 28px;
    box-shadow: 0 2px 16px rgba(0,0,0,0.03);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    min-height: 280px;
}

.rula-page .testimonial-card:hover {
    box-shadow: 0 8px 32px rgba(0,0,0,0.06);
    transform: translateY(-4px);
    border-color: transparent;
}

.rula-page .testimonial-avatar {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    overflow: hidden;
    margin: -56px auto 12px;
    border: 3px solid #fff;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    position: relative;
    background: #e8e8e8;
}

.rula-page .google-badge {
    position: absolute;
    bottom: -2px;
    right: -2px;
    width: 20px;
    height: 20px;
    background: #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 1px 4px rgba(0,0,0,0.15);
}

.rula-page .google-badge svg {
    width: 14px;
    height: 14px;
}

.rula-page .testimonial-name {
    text-align: center;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 4px;
}

.rula-page .testimonial-time {
    text-align: center;
    font-size: 0.78rem;
    color: var(--text-faint);
    margin-bottom: 12px;
}

.rula-page .testimonial-stars {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2px;
    margin-bottom: 14px;
}

.rula-page .testimonial-stars svg {
    width: 16px;
    height: 16px;
    fill: #F59E0B;
}

.rula-page .testimonial-text-wrapper {
    flex: 1;
    position: relative;
}

.rula-page .testimonial-text {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--text-body);
    display: -webkit-box;
    -webkit-line-clamp: 4;
    -webkit-box-orient: vertical;
    overflow: hidden;
    direction: rtl;
}

.rula-page .testimonial-text.expanded {
    -webkit-line-clamp: unset;
}

.rula-page .expand-btn {
    background: none;
    border: none;
    color: var(--primary);
    font-size: 0.82rem;
    font-weight: 600;
    cursor: pointer;
    padding: 4px 0;
    font-family: 'Tajawal', sans-serif;
    display: none;
}

.rula-page .expand-btn:hover {
    text-decoration: underline;
}

.rula-page .expand-btn.visible {
    display: inline-block;
}

.rula-page .slider-dots {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-top: 24px;
}

.rula-page .slider-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: none;
    background: #ddd;
    cursor: pointer;
    padding: 0;
    transition: all 0.2s ease;
}

.rula-page .slider-dot.active {
    background: var(--primary);
    width: 24px;
    border-radius: 4px;
}

/* Newsletter Section */
.rula-page .newsletter-section-new {
    padding: 48px 0;
    background: var(--bg-cream);
}

.rula-page .newsletter-card-new {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    border-radius: 32px;
    padding: 56px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 56px;
    align-items: center;
    position: relative;
    overflow: hidden;
}

.rula-page .newsletter-card-new::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255,255,255,0.06) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
}

.rula-page .newsletter-card-new::after {
    content: '';
    position: absolute;
    bottom: -30%;
    left: -10%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(255,255,255,0.04) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
}

.rula-page .newsletter-right {
    position: relative;
    z-index: 1;
}

.rula-page .newsletter-title-new {
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 800;
    color: #fff;
    margin-bottom: 16px;
    line-height: 1.3;
}

.rula-page .newsletter-text-new {
    color: rgba(255,255,255,0.85);
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 28px;
}

.rula-page .newsletter-perks {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.rula-page .perk {
    display: flex;
    align-items: center;
    gap: 10px;
    color: rgba(255,255,255,0.9);
    font-size: 0.92rem;
    font-weight: 500;
}

.rula-page .perk svg {
    flex-shrink: 0;
    color: var(--highlight-green);
}

.rula-page .newsletter-left {
    position: relative;
    z-index: 1;
}

.rula-page .newsletter-form-new {
    background: rgba(255,255,255,0.1);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 24px;
    padding: 32px;
}

.rula-page .newsletter-form-new .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 12px;
}

.rula-page .newsletter-form-new .form-group-new {
    margin-bottom: 12px;
}

.rula-page .newsletter-form-new .form-group-new label {
    display: block;
    font-size: 0.82rem;
    font-weight: 600;
    color: rgba(255,255,255,0.9);
    margin-bottom: 6px;
}

.rula-page .newsletter-form-new .form-group-new label .optional {
    color: rgba(255,255,255,0.5);
    font-weight: 400;
}

.rula-page .newsletter-form-new .form-group-new label .required {
    color: var(--highlight-green);
}

.rula-page .newsletter-form-new .form-group-new input {
    width: 100%;
    height: 50px;
    padding: 0 18px;
    border: 1.5px solid rgba(255,255,255,0.15);
    border-radius: 12px;
    font-family: 'Tajawal', sans-serif;
    font-size: 0.92rem;
    background: rgba(255,255,255,0.08);
    color: #fff;
    transition: all 0.2s ease;
    direction: rtl;
    box-sizing: border-box;
}

.rula-page .newsletter-form-new .form-group-new input::placeholder {
    color: rgba(255,255,255,0.4);
}

.rula-page .newsletter-form-new .form-group-new input:focus {
    outline: none;
    border-color: rgba(255,255,255,0.4);
    background: rgba(255,255,255,0.12);
    box-shadow: 0 0 0 3px rgba(255,255,255,0.06);
}

.rula-page .btn-subscribe {
    width: 100%;
    height: 52px;
    border: none;
    border-radius: 50px;
    background: #fff;
    color: var(--primary);
    font-family: 'Tajawal', sans-serif;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    transition: all 0.25s ease;
    margin-top: 8px;
}

.rula-page .btn-subscribe:hover {
    background: var(--highlight-green);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.15);
}

.rula-page .form-note {
    font-size: 0.78rem;
    color: rgba(255,255,255,0.45);
    text-align: center;
    margin-top: 12px;
    margin-bottom: 0;
}

/* Testimonials & Newsletter Responsive */
@media (max-width: 900px) {
    .rula-page .testimonials-section {
        padding: 56px 0;
    }
    .rula-page .testimonial-card {
        flex: 0 0 calc(50% - 12px);
        min-width: 280px;
    }
    .rula-page .newsletter-card-new {
        grid-template-columns: 1fr;
        gap: 40px;
        padding: 40px 28px;
    }
    .rula-page .slider-arrow {
        display: none;
    }
}

@media (max-width: 600px) {
    .rula-page .testimonial-card {
        flex: 0 0 calc(100% - 0px);
        min-width: 280px;
    }
    .rula-page .slider-track {
        gap: 16px;
    }
    .rula-page .newsletter-card-new {
        padding: 32px 20px;
        border-radius: 24px;
    }
    .rula-page .newsletter-form-new {
        padding: 24px 18px;
    }
    .rula-page .newsletter-form-new .form-row {
        grid-template-columns: 1fr;
    }
    .rula-page .testimonials-header {
        margin-bottom: 32px;
    }
}

/* ========================================
   Blog Pages
   ======================================== */
.rula-page .blog-page {
    padding: 60px 0 100px;
}

.rula-page .blog-header {
    text-align: center;
}

.rula-page .blog-header h1 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 8px;
}

.rula-page .blog-header p {
    font-size: 1.1rem;
    color: var(--text-muted);
}

.rula-page .blog-layout {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 48px;
    align-items: start;
}

.rula-page .blog-main {
    min-width: 0;
}

.rula-page .blog-list {
    max-width: 100%;
    margin: 0;
}

.rula-page .blog-item {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: 28px;
    padding: 28px 0;
    border-bottom: 1px solid var(--border);
    align-items: center;
}

.rula-page .blog-item:last-child {
    border-bottom: none;
}

.rula-page .blog-item-img {
    border-radius: 12px;
    overflow: hidden;
    aspect-ratio: 4/3;
}

.rula-page .blog-item-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.rula-page .blog-item:hover .blog-item-img img {
    transform: scale(1.05);
}

.rula-page .blog-item-content {
    padding: 8px 0;
}

.rula-page .blog-item-cat {
    display: inline-block;
    background: rgba(45, 82, 64, 0.1);
    color: var(--primary-dark);
    font-size: 0.75rem;
    font-weight: 600;
    padding: 4px 12px;
    border-radius: 50px;
    margin-bottom: 10px;
}

.rula-page .blog-item-content h2 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 10px;
    line-height: 1.4;
}

.rula-page .blog-item-content h2 a {
    color: var(--text);
    text-decoration: none;
    transition: color 0.2s;
}

.rula-page .blog-item-content h2 a:hover {
    color: var(--primary);
}

.rula-page .blog-item-content p {
    font-size: 0.95rem;
    color: var(--text-body);
    line-height: 1.7;
    margin-bottom: 12px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.rula-page .blog-item-date {
    font-size: 0.82rem;
    color: #A8A8A8;
}

.rula-page .blog-sidebar {
    position: sticky;
    top: 24px;
}

.rula-page .sidebar-widget {
    background: #fff;
    border: 1px solid var(--border-light);
    border-radius: 16px;
    padding: 24px;
    margin-bottom: 24px;
}

.rula-page .sidebar-widget h3 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 2px solid var(--primary-light);
}

.rula-page .toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.rula-page .toc-list li {
    padding: 6px 0;
    border-bottom: 1px solid var(--border-light);
    line-height: 1.5;
}

.rula-page .toc-list li:last-child {
    border-bottom: none;
}

.rula-page .toc-list a {
    color: var(--text-body);
    font-size: 0.85rem;
    text-decoration: none;
    display: block;
    padding: 4px 0;
    transition: color 0.2s;
}

.rula-page .toc-list a:hover {
    color: var(--brand-green);
}

.rula-page .toc-list .toc-h3 {
    padding-right: 16px;
    font-size: 0.8rem;
}

/* Author Widget */
.rula-page .sidebar-author {
    text-align: center;
}

.rula-page .sidebar-author-img {
    width: 160px;
    margin: 0 auto 12px;
    border-radius: 12px;
    overflow: hidden;
    border: 3px solid var(--brand-light-green);
}

.rula-page .sidebar-author-img img {
    width: 100%;
    height: auto;
    display: block;
}

.rula-page .sidebar-author h4 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text);
}

.rula-page .sidebar-author p {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin: 8px 0;
    line-height: 1.6;
}

/* Categories Widget */
.rula-page .sidebar-categories ul {
    list-style: none;
}

.rula-page .sidebar-categories li {
    padding: 10px 0;
    border-bottom: 1px solid var(--border-light);
}

.rula-page .sidebar-categories li:last-child {
    border-bottom: none;
}

.rula-page .sidebar-categories a {
    display: flex;
    justify-content: space-between;
    color: var(--text-body);
    font-size: 0.9rem;
    transition: color 0.3s ease;
}

.rula-page .sidebar-categories a:hover {
    color: var(--brand-green);
}

.rula-page .sidebar-categories .cat-count {
    color: var(--text-faint);
    font-size: 0.8rem;
}

/* Sidebar CTA */
.rula-page .sidebar-cta {
    background: linear-gradient(135deg, var(--brand-green) 0%, #4A7A5E 100%);
    border: none;
    text-align: center;
    color: #fff;
}

.rula-page .sidebar-cta h3 {
    color: #fff;
    border-bottom-color: rgba(255,255,255,0.2);
}

.rula-page .sidebar-cta p {
    font-size: 0.88rem;
    color: rgba(255,255,255,0.85);
    margin-bottom: 20px;
    line-height: 1.6;
}

.rula-page .sidebar-cta .btn {
    display: inline-block;
    background: #fff;
    color: var(--brand-green);
    padding: 12px 28px;
    border-radius: 50px;
    font-weight: 700;
    font-size: 0.88rem;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.rula-page .sidebar-cta .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.15);
}

/* Blog Post Page */
.rula-page .blog-post-page {
    padding: 40px 0 100px;
}

.rula-page .breadcrumb {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 32px;
    font-size: 0.88rem;
    color: var(--text-muted);
}

.rula-page .breadcrumb a {
    color: var(--primary);
    text-decoration: none;
}

.rula-page .breadcrumb a:hover {
    text-decoration: underline;
}

.rula-page .post-header {
    text-align: center;
    margin-bottom: 48px;
    padding-bottom: 32px;
    border-bottom: 1px solid var(--border);
}

.rula-page .post-category {
    display: inline-block;
    background: rgba(45, 82, 64, 0.1);
    color: var(--primary-dark);
    font-size: 0.8rem;
    font-weight: 600;
    padding: 6px 16px;
    border-radius: 50px;
    margin-bottom: 16px;
}

.rula-page .post-title {
    font-size: 2.4rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 12px;
    line-height: 1.3;
}

.rula-page .post-subtitle {
    font-size: 1.15rem;
    color: #5A5A5A;
    margin-bottom: 20px;
}

.rula-page .post-meta {
    display: flex;
    justify-content: center;
    gap: 24px;
    font-size: 0.9rem;
    color: #9A9A9A;
}

.rula-page .post-content {
    max-width: 720px;
    margin: 0 auto;
}

.rula-page .post-content p {
    font-size: 1.08rem;
    line-height: 1.9;
    color: #3A3A3A;
    margin-bottom: 20px;
}

.rula-page .post-content h2 {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--text);
    margin: 40px 0 20px;
}

.rula-page .post-content h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: #2A2A2A;
    margin: 28px 0 14px;
}

.rula-page .post-content ul {
    margin: 0 0 20px 0;
    padding-right: 24px;
}

.rula-page .post-content li {
    font-size: 1.05rem;
    line-height: 1.8;
    color: #3A3A3A;
    margin-bottom: 8px;
}

.rula-page .post-questions {
    background: #F8F9F7;
    border-radius: 12px;
    padding: 20px 24px;
    margin: 24px 0;
}

.rula-page .post-cta {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    border-radius: 16px;
    padding: 32px;
    text-align: center;
    margin-top: 48px;
}

.rula-page .post-cta p {
    color: rgba(255,255,255,0.9);
    margin-bottom: 20px;
}

.rula-page .post-cta .btn {
    background: #fff;
    color: var(--primary);
    padding: 14px 32px;
    border-radius: 50px;
    font-weight: 700;
    text-decoration: none;
    display: inline-block;
    transition: transform 0.2s, box-shadow 0.2s;
}

.rula-page .post-cta .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.15);
}

.rula-page .post-share {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin-top: 48px;
    padding-top: 32px;
    border-top: 1px solid var(--border);
}

.rula-page .post-share a {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #E0E0E0;
    border-radius: 50%;
    color: #5A5A5A;
    transition: all 0.2s;
}

.rula-page .post-share a:hover {
    background: var(--primary);
    border-color: var(--primary);
    color: #fff;
}

/* Post Layout with Sidebar */
.rula-page .post-layout {
    display: grid;
    grid-template-columns: 1fr 260px;
    gap: 40px;
    align-items: start;
}

.rula-page .post-main {
    min-width: 0;
}

.rula-page .post-sidebar {
    position: sticky;
    top: 24px;
}

@media (max-width: 900px) {
    .rula-page .post-layout {
        grid-template-columns: 1fr;
    }
    .rula-page .post-sidebar {
        position: static;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
    }
    .rula-page .post-sidebar .sidebar-widget {
        margin-bottom: 0;
    }
    .rula-page .post-sidebar .sidebar-cta {
        grid-column: 1 / -1;
    }
}

@media (max-width: 600px) {
    .rula-page .post-sidebar {
        grid-template-columns: 1fr;
    }
}

/* Latest Articles Widget */
.rula-page .sidebar-latest ul {
    list-style: none;
}

.rula-page .sidebar-latest li {
    margin-bottom: 14px;
}

.rula-page .sidebar-latest li:last-child {
    margin-bottom: 0;
}

.rula-page .sidebar-latest a {
    display: flex;
    gap: 12px;
    text-decoration: none;
    align-items: center;
}

.rula-page .sidebar-latest .latest-img {
    width: 80px;
    height: 80px;
    border-radius: 10px;
    overflow: hidden;
    flex-shrink: 0;
    position: relative;
}

.rula-page .sidebar-latest .latest-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.rula-page .sidebar-latest h4 {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text);
    line-height: 1.5;
    flex: 1;
}

/* Post Courses Widget */
.rula-page .post-courses-widget {
    background: var(--brand-off-white);
    border-radius: 24px;
    padding: 36px;
    margin-top: 56px;
}

.rula-page .post-courses-widget h2 {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--text);
    text-align: center;
    margin-bottom: 4px;
}

.rula-page .post-courses-widget > p {
    text-align: center;
    color: var(--text-muted);
    font-size: 0.95rem;
    margin-bottom: 28px;
}

.rula-page .post-courses-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
}

.rula-page .post-course-card {
    display: flex;
    align-items: center;
    gap: 16px;
    background: #fff;
    border-radius: 10px;
    padding: 16px;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.rula-page .post-course-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
}

.rula-page .post-course-card img {
    width: 70px;
    height: 70px;
    border-radius: 10px;
    object-fit: cover;
    flex-shrink: 0;
}

.rula-page .post-course-info h4 {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 4px;
}

.rula-page .post-course-info .course-price {
    font-size: 0.82rem;
    color: var(--brand-green);
    font-weight: 600;
}

@media (max-width: 600px) {
    .rula-page .post-courses-grid {
        grid-template-columns: 1fr;
    }
    .rula-page .post-courses-widget {
        padding: 24px 20px;
    }
}

@media (max-width: 700px) {
    .rula-page .post-title {
        font-size: 1.8rem;
    }
    .rula-page .post-subtitle {
        font-size: 1rem;
    }
    .rula-page .post-meta {
        flex-direction: column;
        gap: 4px;
    }
    .rula-page .post-content h2 {
        font-size: 1.4rem;
    }
    .rula-page .blog-item {
        grid-template-columns: 1fr;
        gap: 16px;
    }
    .rula-page .blog-item-img {
        aspect-ratio: 16/9;
        max-height: 200px;
    }
    .rula-page .blog-header h1 {
        font-size: 2rem;
    }
}

@media (max-width: 900px) {
    .rula-page .blog-layout {
        grid-template-columns: 1fr;
    }
    .rula-page .blog-sidebar {
        position: static;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
    }
    .rula-page .blog-sidebar .sidebar-widget {
        margin-bottom: 0;
    }
    .rula-page .blog-sidebar .sidebar-cta {
        grid-column: 1 / -1;
    }
}

@media (max-width: 600px) {
    .rula-page .blog-sidebar {
        grid-template-columns: 1fr;
    }
}

/* ========================================
   Team Page
   ======================================== */
.rula-page .team-hero {
    background: linear-gradient(135deg, #F7F9F5 0%, var(--primary-light) 100%);
    padding: 80px 0 60px;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.rula-page .team-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 20% 50%, rgba(95,143,115,0.04) 0%, transparent 60%);
}

.rula-page .team-hero-content {
    position: relative;
    z-index: 1;
    max-width: 640px;
    margin: 0 auto;
}

.rula-page .team-hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(95,143,115,0.1);
    color: var(--brand-green);
    font-size: 0.85rem;
    font-weight: 600;
    padding: 8px 20px;
    border-radius: 50px;
    margin-bottom: 20px;
}

.rula-page .team-hero-title {
    font-size: 2.6rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 12px;
    line-height: 1.2;
}

.rula-page .team-hero-desc {
    font-size: 1.1rem;
    color: var(--text-muted);
    line-height: 1.7;
}

.rula-page .team-divider {
    width: 60px;
    height: 3px;
    background: var(--brand-gold);
    border-radius: 3px;
    margin: 24px auto 0;
}

/* Rula Feature Section */
.rula-page .team-section {
    padding: 80px 0;
    background: var(--brand-off-white);
    position: relative;
}

.rula-page .rula-feature {
    display: grid;
    grid-template-columns: 340px 1fr;
    gap: 48px;
    align-items: center;
    background: #fff;
    border-radius: 24px;
    padding: 40px;
    box-shadow: var(--shadow-lg);
    margin-bottom: 64px;
}

.rula-page .rula-image-wrapper {
    position: relative;
}

.rula-page .rula-image-wrapper img {
    width: 100%;
    border-radius: 16px;
    box-shadow: var(--shadow-md);
}

.rula-page .rula-badge {
    position: absolute;
    bottom: -8px;
    right: -8px;
    background: var(--brand-gold);
    color: var(--text);
    font-size: 0.8rem;
    font-weight: 700;
    padding: 6px 16px;
    border-radius: 50px;
    box-shadow: 0 4px 12px rgba(235,194,82,0.3);
}

.rula-page .rula-info h2 {
    font-size: 2rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 4px;
}

.rula-page .rula-role {
    font-size: 1rem;
    color: var(--brand-green);
    font-weight: 600;
    margin-bottom: 16px;
}

.rula-page .rula-text {
    font-size: 1rem;
    line-height: 1.8;
    color: var(--text-body);
    margin-bottom: 24px;
}

.rula-page .rula-credentials {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.rula-page .rula-credential {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--brand-off-white);
    border: 1px solid var(--border);
    padding: 8px 16px;
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--text-body);
}

.rula-page .rula-credential svg {
    width: 16px;
    height: 16px;
    color: var(--brand-green);
    flex-shrink: 0;
}

/* Team Grid */
.rula-page .team-grid-new {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
}

.rula-page .team-member-card {
    background: #fff;
    border-radius: 16px;
    padding: 32px 20px 28px;
    text-align: center;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--border-light);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.rula-page .team-member-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
}

.rula-page .team-avatar {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    overflow: hidden;
    margin: 0 auto 16px;
    border: 3px solid var(--brand-light-green);
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--brand-off-white);
}

.rula-page .team-avatar img {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.rula-page .member-name {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 4px;
}

.rula-page .member-role {
    font-size: 0.85rem;
    color: var(--brand-green);
    font-weight: 600;
    margin-bottom: 10px;
}

.rula-page .member-bio {
    font-size: 0.88rem;
    color: var(--text-muted);
    line-height: 1.6;
}

/* Team CTA */
.rula-page .team-cta {
    padding: 0 0 80px;
    background: var(--brand-off-white);
}

@media (max-width: 900px) {
    .rula-page .rula-feature {
        grid-template-columns: 1fr;
        padding: 28px;
        gap: 28px;
    }
    .rula-page .rula-image-wrapper {
        max-width: 280px;
        margin: 0 auto;
    }
    .rula-page .team-grid-new {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 600px) {
    .rula-page .team-hero-title {
        font-size: 1.8rem;
    }
    .rula-page .team-hero {
        padding: 60px 0 40px;
    }
    .rula-page .team-section {
        padding: 48px 0;
    }
    .rula-page .rula-credentials {
        flex-direction: column;
    }
    .rula-page .team-grid-new {
        grid-template-columns: 1fr;
    }
}

/* ========================================
   Policy Pages
   ======================================== */
.rula-page .policy-content {
    max-width: 720px;
    margin: 0 auto;
    padding: 48px 0;
}

.rula-page .policy-content h1 {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--brand-green);
    margin-bottom: 32px;
    padding-bottom: 16px;
    border-bottom: 3px solid var(--brand-light-green);
}

.rula-page .policy-content h2 {
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--text);
    margin: 28px 0 12px;
}

.rula-page .policy-content p {
    font-size: 1rem;
    line-height: 1.9;
    color: var(--text-body);
    margin-bottom: 16px;
}

.rula-page .policy-content ul {
    padding-right: 24px;
    margin-bottom: 16px;
}

.rula-page .policy-content li {
    font-size: 0.95rem;
    line-height: 1.8;
    color: var(--text-body);
    margin-bottom: 6px;
}

/* ========================================
   Global Overrides
   ======================================== */
.rula-page table,
.rula-page tr,
.rula-page td {
    border: none;
    background: transparent;
}

.rula-page *:where(:not(iframe)):not(html):not(body)::before,
.rula-page *:where(:not(iframe)):not(html):not(body)::after {
    display: revert;
}

/* Force WoodMart VC row to be full-width */
.rula-page .vc_row {
    margin: 0 !important;
}

.rula-page .wpb_column {
    margin-bottom: 0 !important;
}

/* Remove any weird VC padding */
.rula-page .vc_row[data-vc-full-width] {
    overflow: visible !important;
}

/* Fix Tajawal font */
@font-face {
    font-family: 'Tajawal';
    font-style: normal;
    font-weight: 300;
    src: url(https://fonts.gstatic.com/s/tajawal/v9/Iurf6YBj_oCad4k1l5qjHrRpiYlJ.woff2) format('woff2');
    unicode-range: U+0600-06FF, U+200C-200E, U+2010-2011, U+204F, U+2E41, U+FB50-FDFF, U+FE80-FEFC;
}

@font-face {
    font-family: 'Tajawal';
    font-style: normal;
    font-weight: 400;
    src: url(https://fonts.gstatic.com/s/tajawal/v9/Iura6YBj_oCad4k1nzSBC45I.woff2) format('woff2');
    unicode-range: U+0600-06FF, U+200C-200E, U+2010-2011, U+204F, U+2E41, U+FB50-FDFF, U+FE80-FEFC;
}

@font-face {
    font-family: 'Tajawal';
    font-style: normal;
    font-weight: 500;
    src: url(https://fonts.gstatic.com/s/tajawal/v9/Iurf6YBj_oCad4k1l8qjHrRpiYlJ.woff2) format('woff2');
    unicode-range: U+0600-06FF, U+200C-200E, U+2010-2011, U+204F, U+2E41, U+FB50-FDFF, U+FE80-FEFC;
}

@font-face {
    font-family: 'Tajawal';
    font-style: normal;
    font-weight: 700;
    src: url(https://fonts.gstatic.com/s/tajawal/v9/Iurf6YBj_oCad4k1l4qjHrRpiYlJ.woff2) format('woff2');
    unicode-range: U+0600-06FF, U+200C-200E, U+2010-2011, U+204F, U+2E41, U+FB50-FDFF, U+FE80-FEFC;
}

@font-face {
    font-family: 'Tajawal';
    font-style: normal;
    font-weight: 800;
    src: url(https://fonts.gstatic.com/s/tajawal/v9/Iurf6YBj_oCad4k1l4qjHrRpiYlJ.woff2) format('woff2');
    unicode-range: U+0600-06FF, U+200C-200E, U+2010-2011, U+204F, U+2E41, U+FB50-FDFF, U+FE80-FEFC;
}

/* Appointment Page */
.rula-page .appointment-hero {
    background: linear-gradient(135deg, #F7F9F5 0%, var(--primary-light) 100%);
    padding: 80px 0 60px;
    text-align: center;
}

.rula-page .appointment-hero h1 {
    font-size: 2.6rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 12px;
}

.rula-page .appointment-hero p {
    font-size: 1.1rem;
    color: var(--text-muted);
    max-width: 600px;
    margin: 0 auto;
}

.rula-page .appointment-options {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    padding: 60px 0;
}

.rula-page .appointment-card {
    background: #fff;
    border-radius: 24px;
    padding: 40px 32px;
    text-align: center;
    border: 1px solid var(--border-light);
    box-shadow: var(--shadow-sm);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.rula-page .appointment-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
}

.rula-page .appointment-card .icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--brand-off-white);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 20px;
}

.rula-page .appointment-card .icon svg {
    width: 32px;
    height: 32px;
    color: var(--brand-green);
}

.rula-page .appointment-card h3 {
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 8px;
}

.rula-page .appointment-card p {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin-bottom: 20px;
    line-height: 1.6;
}

.rula-page .appointment-card .btn-whatsapp {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #25D366;
    color: #fff;
    padding: 12px 28px;
    border-radius: 50px;
    font-weight: 600;
    font-size: 0.92rem;
    text-decoration: none;
    transition: all 0.3s ease;
}

.rula-page .appointment-card .btn-whatsapp:hover {
    background: #128C7E;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(37,211,102,0.3);
}

@media (max-width: 768px) {
    .rula-page .appointment-options {
        grid-template-columns: 1fr;
    }
    .rula-page .appointment-hero h1 {
        font-size: 1.8rem;
    }
}

/* Fix for WoodMart - remove unwanted margins/paddings */
.rula-page p:empty {
    display: none;
}

.rula-page br {
    display: revert;
}
